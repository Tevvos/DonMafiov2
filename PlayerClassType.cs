using Fusion;

public enum PlayerClassType : byte
{
    None = 0,
    Mastodonte = 1, // +50 PV max
    DogOfWar = 2,  // + munitions de départ
    Furtif = 3     // vitesse de déplacement accrue
}
